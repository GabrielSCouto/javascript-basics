//GETS INT AND RETURNS TRUE IF EVEN AND FALSE IF ODD

function isPar(num: number): any{
    if (num % 2 === 0) {
        return "true"
    }
    else {
        return "false"
    }
}

console.log(isPar(11))
